import { Button } from "@govje-frontend/react";

const component = () => <Button variant="primary" label="Connect to SignVideo" />

const react = () => `<Button variant="primary" label="Connect to SignVideo" />`

const html = () => `<button class="govje-button govje-button--primary">Connect to SignVideo</button>`

export default { component, html, react }